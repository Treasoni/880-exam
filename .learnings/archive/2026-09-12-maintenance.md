# 2026-09-12 经验库维护归档

## 处理原因
本次公式渲染问题属于既有 LaTeX 转义错误的复发；已修复源头校验并验证。为保持活跃经验文件低于 100 行，归档已验证的旧记录。

## LEARNINGS.md：已验证的旧记录压缩归档

## 2026-09-02

### 解析结果中的字面量换行

**类别**：best_practice
**优先级**：high
**状态**：pending
**范围**：880 / 解析合并 / Obsidian Markdown

**摘要**：结构化提取结果中的 `\\n` 可能只是两个字面量字符；在合并层解码为真实换行，同时避免误伤 `\\neq` 等 LaTeX 命令。

**详情**：
- 事实：paper-02 第 2 道解答题的解析被压成一行，公式块无法正常显示。
- 根因：提取结果中的换行以字面量 `\\n` 存储，生成答案卷时未做归一化。
- 下次做法：在解析合并边界统一归一化，并用回归测试覆盖“解码换行但保留 LaTeX 命令”。

---

## 2026-09-12

### 解析写入前的 Obsidian 公式防线

**类别**：correction | best_practice
**优先级**：high
**状态**：pending
**范围**：880-wrongbook / Obsidian Markdown

**摘要**：独立公式统一用 `$$...$$`，且生成错题本前必须拒绝 `\[...\]` 定界符与反斜杠转义产生的控制字符。

**详情**：
- 事实：本次新增解析先产生了 `\[...\]`，并短暂出现 `\x0c`，导致 Obsidian 渲染异常；`scripts/lint_content.py` 已复现并在修复后通过。
- 下次做法：先更新索引事实源，生成器写文件前做格式/控制字符校验，重建后再运行 lint。

---

---

## ERRORS.md：已修复的旧 LaTeX 转义记录归档

## 2026-09-03


### 880-analysis：Python 字符串转义破坏了 LaTeX 公式

**错误**：写入错因分析的建议字段时，普通 Python 字符串将 `\frac` 中的 `\f` 解释为换页控制字符，错题本中出现缺失反斜杠、渲染损坏的 `frac`（底层含换页控制字符）。

**触发场景**：把带 LaTeX 的建议文本写入 `workspace/records/analysis.json` 后重建错题本。

**根因**：使用普通字符串承载反斜杠命令，没有使用 raw string 或显式双反斜杠。

**修复**：改用 raw string 重写建议字段，重新生成错题本，并检查 `analysis.json` 与渲染结果。

**预防措施**：
- 脚本写入 LaTeX 时使用 raw string 或显式转义反斜杠。
- 写入后检查控制字符（特别是 `\x0c`），并 grep 目标 callout 验证公式未损坏。

---

---

