# 错误日志

---

_最后更新：2026-08-11_

## 2026-08-11

### 一次性脚本：python -c 漏导入 Path 导致 NameError

**错误**：重建 paper-01 答案卷的一次性 `python -c` 片段第 19 行 `NameError: name 'Path' is not defined`，脚本跑挂一次。

**触发场景**：用 `python -c` 内联运行多步脚本时。

**根因**：内联片段顶部忘了 `from pathlib import Path`，直接用了 `Path()`。

**修复**：
- 补上 `from pathlib import Path` 后重跑成功，答案卷按预期重建。

**预防措施**：
- 用 `python -c` 跑一次性脚本前，先把所有 import 在开头写全再执行；步骤较多时写成临时 `.py` 文件再运行，避免内联片段漏导入。

---

### 判分卡解析：QHEAD_RE 粗体题头正则写错括号内句点

**错误**：`parse_grading_card()` 新格式题头正则 `^\*\*(\d+)\*\*\.\s*答案` 匹配 `**1.** 答案：D` 失败，新格式判分卡解析返回空 grading。

**触发场景**：为判分卡任务清单格式写 `**N.** 答案：…` 题头识别正则时。

**根因**：`**1.**` 中数字后的 `.` 位于粗体闭合标记内侧（`**` + `1.` + `**`），正则却写成 `\*\*(\d+)\*\*\.`（误以为 `.` 在 `**` 之后）。

**修复**：
- 改为 `^\*\*(\d+)\.\*\*\s*答案`，自测单勾/多勾/全空/旧格式全部通过。

**预防措施**：
- 解析 `**text.**` 这类粗体时，闭合 `**` 在句点之后；
- 给解析器写单勾/多勾/全空/旧格式的单元自测再落地。

---

### 校验正则：`\d` 匹配不到两位数行号（`一10`）误报少一格

**错误**：用正则 `\| (?:一|二|三)\d \|` 统计判分表勾选行，结果 19 格而非 20 格，一度以为判分卡漏了状态。

**触发场景**：校验判分卡是否每题都勾了状态，用正则扫描 Markdown 表格行时。

**根因**：`\d` 只匹配单个数字；`| 一10 |` 是两位数，`三\d ` 无法匹配，导致该行被漏数。实为误报，判分卡本身无错。

**修复**：
- 改用 `sed` 直接读取判分表确认全部 20 格正确，实数据无问题。

**预防措施**：
- 正则匹配数字一律写 `\d+` 而非 `\d`；
- 对「计数/校验类」结论，先用 `sed`/`grep` 直接看原始文件确认，再下结论，避免仅凭正则扫描误判。

---


## 2026-08-11

### 880-wrongbook：只补答案卷未同步错题本事实源

**错误**：用户要求补充解析时，我先只改了答案卷，交付时没有把同一补充同步到错题本，用户追问“错题本那里不加？”。

**触发场景**：错题本条目的解析来自题目索引/生成脚本，而用户正在答案卷中选中一段解析要求补充。

**根因**：把当前编辑文件当成唯一输出，忽略了错题本是由 `workspace/question-index.json` 和判分记录重生成的派生产物。

**修复**：
- 将“积分中值定理这一步”的补充写入 `workspace/question-index.json` 对应题目解析。
- 运行 `python3 scripts/wrong_book.py` 重生成错题本，并 grep 验证 callout 已出现在对应题目下。

**预防措施**：
- 对已进入错题本的题目做解析补充时，必须同步更新错题本事实源并重生成错题本；不能只改当前答案卷。

---


## 2026-08-11

### digest：f-string 中未转义机器 token 导致脚本失败

**错误**：压缩 `.learnings/LEARNINGS.md` 的一次性 Python 脚本里，在 f-string 文本中直接写 `{not_started}` / `{complete}`，触发 `NameError`；修复脚本时又在另一个 f-string 错误日志模板里重复触发一次。

**触发场景**：用 f-string 生成包含花括号机器 token 的 Markdown 内容。

**根因**：f-string 会把单花括号当成表达式求值；需要写 `{{...}}`、使用 `.format()` 参数占位，或完全避免 f-string。

**修复**：
- 改用字符串拼接和普通三引号文本补写错误日志与规则。

**预防措施**：
- 生成包含 `{token}`、JSON、模板占位符的长文本时，避免直接使用 f-string；必须保留花括号时先显式转义或用普通字符串。

---
